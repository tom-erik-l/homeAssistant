counter:
  # https://www.home-assistant.io/integrations/counter/
  stue:
    initial: 0
    step: 1
  spisestue:
    initial: 0
    step: 1
  sovevaerelse:
    initial: 0
    step: 1
  kokken:
    initial: 0
    step: 1
  badevaerelse:
    initial: 0
    step: 1
  gang:
    initial: 0
    step: 1
  noop:
    initial: 0
    step: 1

# what is the scope in script / blueprint / automation? Are these only local, or are they global across script invocations?
# To prevent the need for creating multiple input_booleans script mode is set to "queued" or "single"
input_boolean:
  disableincrement: #Internal switch for the automation to trigger -> temporarily disable the incrementation of lights turning off / on
    initial: "off"
  disableincrementstue: #Internal switch for the automation to trigger -> temporarily disable the incrementation of lights turning off / on
    initial: "off"
  disableincrementspisestue: #Internal switch for the automation to trigger -> temporarily disable the incrementation of lights turning off / on
    initial: "off"
  disableincrementkokken: #Internal switch for the automation to trigger -> temporarily disable the incrementation of lights turning off / on
    initial: "off"
  disableincrementsovevaerelse: #Internal switch for the automation to trigger -> temporarily disable the incrementation of lights turning off / on
    initial: "off"
  disableincrementbadevaerelse: #Internal switch for the automation to trigger -> temporarily disable the incrementation of lights turning off / on
    initial: "off"
  disableincrementgang: #Internal switch for the automation to trigger -> temporarily disable the incrementation of lights turning off / on
    initial: "off"